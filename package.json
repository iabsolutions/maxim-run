{
    "name": "maxim-run",
    "version": "1.0.0",
    "description": "GitHub Action to run a test on Maxim Cloud",
    "main": "index.js",
    "scripts": {
        "build": "npm install && ncc build index.js -o dist",
        "push": "git tag -d v1 && git tag v1 && git push --force origin v1"
    },
    "dependencies": {
      "@actions/core": "^1.6.0",
      "@actions/github": "^5.0.0",
      "axios": "^0.21.1"
    },
    "devDependencies": {
      "@vercel/ncc": "^0.33.3"
    }
  }